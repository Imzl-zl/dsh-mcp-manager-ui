- 新增第三类失败「作用域故障」。它比"读不到工具"隐蔽得多：作用域标签认不出时 `tools.schemas(scopeKey)` 会退回全局层，工具其实对所有会话都可见，而面板显示的是「已连接 N 工具」——现在直接对比全局视图把这种情况判出来
- 去掉两个死引用（`@deepseek-ai/dsh-client-runtime` 上游已删除，`@deepseek-ai/dsh-client-ui-slots` 从不声明 `dsh.client`）；死引用会被客户端模块系统静默跳过
- 项目工具视图、`tools.get`、`workspaceRegistry.list()` 的读失败不再静默